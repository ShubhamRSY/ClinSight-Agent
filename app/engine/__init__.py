"""Interpretation, aggregation, labeling, citations, and response building.

Submodules:
- ``interpreter`` / ``heuristics`` — NL → search params (LLM + grounding)
- ``study_fields`` — CT.gov field extractors / normalizers
- ``aggregator`` / ``viz_maps`` — deterministic chart rows and encodings
- ``classifier`` / ``labels`` — title/notes (LLM + templates)
- ``citations`` / ``builder`` — deep NCT citations and final response
"""
