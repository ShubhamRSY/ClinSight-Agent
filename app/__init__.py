"""ClinSight Agent package — clinical-trial NL → visualization backend.

Subpackages: ``api`` (CT.gov client), ``engine`` (interpret/aggregate/cite),
``services`` (fetch/filter/cache), ``schemas`` (I/O), ``routers`` (HTTP).
"""
